//setTimeout() -- function allows user to schedule the execution of a 
//                  function after an amount of time
//                      setTimeout(callback, delay);

function startTimer() {
    setTimeout(() => window.alert("Hello"), 3000);
}